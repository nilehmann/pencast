* NavFab should I move the derived to methods in stores, should I make methods in store $derived fields?
* server.ts saveAnnotations returns if activePdf is none, is this correct?
* whiteboard_page_added mesage is weird
* gestures.svelte.ts activeSelectedStrokes can be refactored
* can we use set annotations in active context to avoid functional updates?
* patchPage may no longer be necessary
